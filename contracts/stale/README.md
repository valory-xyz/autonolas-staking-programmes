# stale

Contracts that were written and reviewed but **never deployed to any chain**.

They are kept here rather than deleted so the work is not lost and can be picked up again. They are not
part of the active deployment set.

Distinct from the `deprecated/` directories, which hold contracts that *were* deployed and have since been
replaced — those keep a source of record for code still live on-chain.
